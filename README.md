Quick test repo for potential wrapping rotate methods on slices. 

Run `cargo test` to test that methods are equal to current implementations.

Run `cargo bench` to test speed results on your machine.
